server.port=80

spring.datasource.url= your.test.db.url
spring.datasource.username= your.test.db.username
spring.datasource.password= your.test.db.password
spring.jpa.generate-ddl= true

# This URL prefix property configure URL mappings in html pages
# For Apache Tomcat you need to write war package name as prefix:
# application.url.prefix=/LibrarySystem
application.url.prefix=